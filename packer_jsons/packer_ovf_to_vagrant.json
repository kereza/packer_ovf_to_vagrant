{
    "builders": [
        {
            "type": "amazon-ebs",
            "access_key": "{{user `aws_access_key`}}",
            "secret_key": "{{user `aws_secret_key`}}",
            "region": "{{user `aws_region`}}",
            "source_ami": "ami-003fd56f",
            "ssh_pty": true,
            "instance_type": "t2.micro",
            "ssh_username": "centos",
            "ami_name": "aws_main_{{timestamp}}"
        }
        ],
    "provisioners": [
            {
                "type": "ansible",
                "playbook_file": "./playbook.yml"
            }
        ]
}

